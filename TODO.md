# Auth Redirect & Profile Dropdown Implementation

## Steps
- [x] 1. Update `useAuth.js` — add `login()`, `signup()`, and URL param reading
- [x] 2. Update `LoginPage.jsx` — wire up API call + redirect to `/`
- [x] 3. Update `SignupPage.jsx` — wire up API call + redirect to `/`
- [x] 4. Update `Navbar.jsx` — fix mobile menu for auth-aware links
- [x] 5. Update `LandingPage.jsx` — URL params handled via `useAuth.js`
- [x] 6. Test & verify

